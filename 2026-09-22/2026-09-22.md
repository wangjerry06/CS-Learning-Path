# 今日教训

1. char初始化的时候定义空字符要用'\0'

2. sort的头文件是<algorithm>
3. 循环的时候越界检查要放在前面

4. 在表示大小、下标的时候，变量类型用size_t
5. s.find()用来在string里找子字段，没找到的输出结果是string::npos
6. Find(first,end,value)是在里边找元素，找到的输出是第一个匹配的iterator，要用*i来解引拿值，找不到输出的是last的iterator
